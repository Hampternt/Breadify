//! Breadify — reads a daily bread-order export and prints A4 picking lists.
//!
//! See `docs/print-spec.md` for what the printed page must say, and
//! `docs/excel-format.md` for the shape of the input file.

fn main() {
    println!("breadify {}", env!("CARGO_PKG_VERSION"));
    println!("nothing to do yet — see docs/manifests/ for what is being built");
}
