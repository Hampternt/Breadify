# Pack 6 — Print

**Status:** all 6 items done, reviewed, pack gate green. Merged.
**Container:** Breadify v1 (7 packs).
**Branch:** `pack-6-print`.

## Goal

Step 4: choose what to print, look at it, and get it onto paper.

**Observable when done:** pick routes, see A4 thumbnails and a sheet count the
paginator worked out, then either export a PDF or hand it to the system to
print.

## Items

| # | Item | Done when |
| --- | --- | --- |
| 1 | The route table | One row per route — stops, lines, sheets — each selectable, with select-all and select-none |
| 2 | Thumbnails | The selected routes' sheets drawn as A4 thumbnails from the same display list, scrollable |
| 3 | No sheet figure is a literal | Every count in the step comes from the paginator, including the primary button's label |
| 4 | Export a PDF | A save dialog writes the selected routes; the file opens in a viewer as A4 |
| 5 | Print it | The PDF is written and handed to the system's own handler, which brings the user their printer picker (D18) |
| 6 | The scaling warning | The step says to print at actual size, because a viewer's fit-to-page silently shrinks A4 by about 4 % |

## Ledger

- [x] 1 — The route table · stops, lines, sheets, select all/none
- [x] 2 — Thumbnails · 26 A4 thumbnails from the same display list
- [x] 3 — No sheet figure is a literal · every count from the paginator
- [x] 4 — Export a PDF · save dialog, named by the delivery date
- [x] 5 — Print it · written and handed to the system handler (D18)
- [x] 6 — The scaling warning · in the action bar, on the step

**Deviations:**

- No printer picker inside the app, by **D18**: the PDF is written and handed
  to the system's own handler, which brings the user their printer dialog,
  preview and scaling control. The Windows `PrintDlgW` path stays out of v1.
**Code review:** one pass at high effort. Two findings, both fixed:

1. The thumbnail grid cloned the whole day — 26 pages of positioned
   primitives — on every frame. The day is lent out now, not copied.
2. `sheets_for` reported a route's sheet count from a stale pagination while a
   changed setting was still settling, so a two-sheet route could read as one.
   It returns `None` while stale and the table shows `…`.

**Pack gate:** `./scripts/verify.sh` — fmt, clippy `-D warnings`, build, and the
test suite, all green. The step was opened and looked at: 16 routes
with their real counts, 26 thumbnails, `Print 26 sheets` and `Export PDF`.
